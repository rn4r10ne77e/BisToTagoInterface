<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.geon.bis.link.mapper.BaseInfoVersionMapper">
    <select id="findAllRecent" parameterType="ParamBaseInfoVersion" resultType="ResultBaseInfoVersion">
        select 1;
    </select>
    <select id="findChanged" parameterType="ParamBaseInfoVersion" resultType="ResultBaseInfoVersion">
        select 1;
    </select>
</mapper>
